<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.jay.androidscreencompatibility">

    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>

    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".ui.others.ScreenInfohActivity">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN"/> -->
            <!-- <category android:name="android.intent.category.LAUNCHER"/> -->
            <!-- </intent-filter> -->
        </activity>
        <activity
            android:name=".ui.main.DemoActivity"
            android:theme="@style/AppThemeWithBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.others.LayoutDirectionActivity"
            android:label="LayoutDirectionActivity">
        </activity>
        <activity
            android:name=".ui.others.ConstraintLayoutActivity"
            android:label="ConstraintLayoutActivity">
        </activity>
        <activity
            android:name=".ui.adaptation_dp.DPAdaptActivity"
            android:theme="@style/AppTheme">
        </activity>
        <activity
            android:name=".ui.adaptation_sw.SWAdaptActivity"
            android:theme="@style/AppTheme">
        </activity>
        <activity
            android:name=".ui.adaptation_density.DensityAdaptActivity"
            android:theme="@style/AppTheme">
        </activity>
        <activity android:name=".ui.adaptation_bar.DisplayCutoutActivity">
        </activity>
        <activity android:name=".ui.adaptation_view.ViewAdaptActivity">
        </activity>
    </application>

</manifest>